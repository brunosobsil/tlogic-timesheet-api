/*
'use strict';

const models = require("../../../orm/models"); // cria tudo com o sync

module.exports = {

  up: function (queryInterface, Sequelize) {
    
    // Cria as tabelas na ordem certa
    //return queryInterface.createTable(models.Usuario.tableName, models.Usuario.attributes)
    //.then(() => queryInterface.createTable(models.Habilidade.tableName, models.Habilidade.attributes))
    //.then(() => queryInterface.createTable(models.UsuarioHabilidade.tableName, models.UsuarioHabilidade.attributes))
    //.then(() => queryInterface.createTable(models.Projeto.tableName, models.Projeto.attributes))
    //.then(() => queryInterface.createTable(models.Atendimento.tableName, models.Atendimento.attributes))
    //.then(() => queryInterface.createTable(models.OrdemServico.tableName, models.OrdemServico.attributes))

  },

  down: function (queryInterface, Sequelize) {
    
  }

};
*/